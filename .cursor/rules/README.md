# Cursor rules (optional)

Add `.mdc` rule files here when you want editor-integrated agents to follow constraints. Reference canonical specs under `docs/` (for example `docs/product/phone-verification-spec.md`) instead of duplicating long product text in rules.
